# 해커톤 프로젝트 운영 규칙

이 저장소에서 해커톤 과제, 대회 링크, 대회 데이터, 학습 코드, 모델 설계 또는 제출 전략을 다룰 때는 아래 규칙을 적용한다.

## 최우선 원칙: 데이터 분석이 모델보다 먼저다

- 새 해커톤 링크나 새 데이터 버전이 주어지면 가장 먼저 `hackathon_data_analyst` 에이전트를 호출하고 완료될 때까지 기다린다.
- 현재 데이터와 일치하는 `DATA_READINESS: READY` 감사 보고서가 없으면 모델 구조 선택, 피처 엔지니어링 구현, 학습, 튜닝, 앙상블 및 제출 코드 작성을 시작하지 않는다.
- `DATA_READINESS: READY`가 확인되면 구현 전에 `hackathon_model_architect` 에이전트를 호출하고 완료될 때까지 기다린다.
- 현재 데이터·대회 규칙·평가지표와 일치하는 `MODEL_PLAN: READY` 설계 보고서가 없으면 모델 구현, 학습, 튜닝, 앙상블 및 제출 코드 작성을 시작하지 않는다.
- `MODEL_PLAN: READY`가 확인되면 `hackathon_experiment_engineer`만 승인된 실험 계획을 구현·실행한다.
- 완료된 실험 결과는 `hackathon_validation_auditor`의 `EXPERIMENT_AUDIT: PASS` 전에는 모델 개선의 근거로 채택하지 않는다.
- 최종 제출 파일은 `hackathon_validation_auditor`의 `SUBMISSION_READY: PASS` 전에는 제출하지 않는다.
- 기존 보고서는 데이터 매니페스트의 파일 크기와 SHA-256이 현재 원본 데이터와 일치할 때만 유효하다. 파일 또는 대회 규칙이 바뀌면 다시 감사한다.
- 분석이 인증, 이용 약관 동의, 접근 권한, 누락 파일 또는 손상 파일 때문에 완료되지 않으면 우회하지 않는다. `BLOCKED`로 보고하고 사용자에게 필요한 조치만 구체적으로 요청한다.
- 사용자가 명시적으로 분석을 생략하라고 해도 생략에 따른 누수·검증 오류 위험을 먼저 알리고, 최소한 스키마·타깃·평가지표·분할·중복·누수 검사는 수행한다.

## 이 프로젝트에서 데이터 분석의 정의

이 프로젝트에서 "데이터 분석"은 실제 데이터 파일에 대한 통계·품질 감사와 해당 문제의 도메인 지식 조사를 합친 단계다. 둘 중 하나만 수행한 결과는 완료로 보지 않는다.

- 대회 주제와 데이터 생성 과정을 이해하는 데 필요한 공식 문서, 표준·가이드라인, 동료평가 논문, 학회·기관·기업의 신뢰할 만한 기술 보고서와 최신 기사를 조사한다.
- 출처 우선순위는 대회 공식 자료와 원 출처, 표준·공식 가이드라인, 동료평가 논문·체계적 문헌고찰, 권위 있는 기술 보고서, 신뢰할 만한 기사 순이다. 블로그·포럼·홍보 자료는 가설 발굴에만 사용하고 핵심 사실의 단독 근거로 사용하지 않는다.
- 도메인 조사는 데이터 생성·라벨링 과정, 알려진 편향과 교란요인, 정상·물리적 범위, 대표적인 신호와 잡음, 기존 평가 관행과 함정, 유사 데이터셋·벤치마크 및 최근 접근법을 다룬다.
- 외부 자료의 주장은 그대로 믿거나 모델 아이디어로 직행하지 않는다. 각 핵심 주장에 대해 `출처 → 이 대회 데이터에서 확인할 검사 → 실제 결과 → 모델링·검증에 미치는 영향`을 연결한다.
- 최신성에 민감한 사실은 조사 시점에 웹에서 확인하고 출판일과 실제 사건·개정일을 구분한다. 모든 핵심 주장에는 직접 확인 가능한 출처 링크와 확인일을 남긴다.
- 서로 충돌하는 연구, 적용 대상이 다른 연구, 관찰 연구와 인과 주장, 기사와 원 논문의 차이를 구분하고 불확실성과 적용 한계를 적는다.
- 문헌에서 소개된 외부 데이터, 사전학습 모델, 전처리 또는 특징이 대회 규칙상 허용되는지 별도로 확인한다. 규칙이 금지한 자료는 분석 지식으로만 참고하고 학습 데이터나 제출 파이프라인에 넣지 않는다.

## 데이터 감사 완료 조건

`hackathon_data_analyst`의 결과는 추측이나 일반론이 아니라 실제 파일을 읽어 산출한 수치와 근거를 포함해야 한다. 최소 완료 조건은 다음과 같다.

1. 대회 목적, 예측 단위, 타깃, 제출 형식, 평가지표, 최적화 방향, 주요 규칙과 마감 정보를 공식 출처로 확인한다.
2. 주최 측이 제공한 데이터와 설명 문서를 내려받고 파일별 출처 URL, 다운로드 시각, 크기, SHA-256, 압축 해제 상태를 매니페스트에 남긴다.
3. train/test/sample submission 및 보조 파일의 역할, 행·열 또는 샘플 수, 스키마, 자료형, 키, 타깃을 식별한다.
4. 결측, 무한값, 상수·준상수, 고유값 수, 중복 행·ID·콘텐츠, 범위 오류, 파싱 실패, 클래스 불균형과 이상치를 수치화한다.
5. train/test 스키마 차이와 분포 이동을 비교하고, 시간·사용자·장치·화자·그룹·원본 콘텐츠 단위의 겹침을 조사한다.
6. 타깃 누수, 사후 정보, 파일명·경로·메타데이터 누수, 중복 샘플 누수, 전처리 누수 가능성을 증거와 함께 점검한다.
7. 평가지표와 데이터 생성 구조에 맞는 검증 단위, 분할 방식, 층화·그룹·시간 처리 및 금지해야 할 분할 방식을 제안한다.
8. 표형·시계열·텍스트·이미지·오디오 등 실제 모달리티에 맞춰 품질을 검사한다. 대용량이라 전체 내용 검사가 어렵다면 전체 메타데이터 검사와 대표 표본 검사를 분리하고 표본 선정법과 한계를 명시한다.
9. 핵심 분포와 품질 문제를 표와 시각화로 남기고, 데이터에서 직접 도출된 모델링 제약과 가설을 구분해 정리한다.
10. 관련 도메인의 데이터 생성 메커니즘, 알려진 교란·편향·평가 함정과 유사 연구를 조사하고, 중요한 외부 지식이 실제 대회 데이터에서도 성립하는지 검사한다.

## 산출물과 원본 보존

- DeepVoice 대회의 전용 프로젝트 루트는 `deepvoice/`다. 데이터, 보고서, 학습 산출물, 캐시를 포함한 신규 폴더를 저장소 최상위에 만들지 않는다.
- 원본은 `deepvoice/data/raw/<source>/<version>/` 아래에 보존하고 수정하거나 덮어쓰지 않는다. 변경된 배포본은 새 버전 디렉터리에 저장한다.
- 중간·가공 데이터는 각각 `deepvoice/data/interim/`, `deepvoice/data/processed/` 아래에 둔다.
- 감사 결과는 `deepvoice/reports/` 아래에 둔다.
- 학습 산출물과 체크포인트는 각각 `deepvoice/artifacts/`, `deepvoice/checkpoints/` 아래에 둔다.
- 최소 산출물은 `data-audit.md`, `domain-research.md`, `data-manifest.csv`, `evidence-map.csv`, 재현 가능한 분석 코드, 그리고 필요한 표·그림이다.
- 보고서 첫 부분에 `DATA_READINESS: READY` 또는 `DATA_READINESS: BLOCKED`를 한 줄로 기록한다.
- `evidence-map.csv`에는 핵심 외부 주장, 출처, 근거 수준, 데이터 검사 방법, 검사 결과, 영향과 한계를 연결해 기록한다.
- 보고서 끝에는 확인한 사실, 아직 확인하지 못한 사항, 치명적 위험, 검증 전략, 모델링 단계가 반드시 지켜야 할 제약을 분리해 적는다.
- 비밀번호, 세션 쿠키, API 토큰 같은 비밀정보는 저장소나 보고서에 기록하지 않는다.

## 모델링 단계 진입

- 기본 에이전트는 `READY` 보고서와 매니페스트를 직접 읽은 뒤 모델링을 시작한다.
- 기본 에이전트는 구현 전에 `hackathon_model_architect`를 호출해 `deepvoice/reports/modeling-plan.md`, `deepvoice/reports/experiment-plan.csv`, `deepvoice/reports/model-decision-log.md`를 작성하게 한다.
- 모델 설계가는 `data-audit.md`, `domain-research.md`, `evidence-map.csv`, `data-manifest.csv`와 대회 평가 계약을 필수 입력으로 사용한다.
- 모델 선택, 피처, 손실함수, 검증 및 실험 우선순위는 데이터·도메인 보고서의 구체적 근거와 연결한다. 모델을 먼저 정한 뒤 근거를 찾지 않는다.
- 검증 설계를 가장 먼저 확정한 다음 단순 기준선, 강한 기준선, 후보 모델, 개선 실험과 앙상블 조건 순으로 설계한다.
- 감사에서 발견한 그룹·시간·중복·누수 제약을 코드와 검증 파이프라인에 강제한다.
- 실제 분석 결과와 충돌하는 관행적 가정은 사용하지 않는다.
- `MODEL_PLAN: READY`는 재현 가능한 검증 명세, 기준선, 근거가 있는 후보 모델, 성공·중단 기준, 계산 자원과 실험 우선순위가 확정된 경우에만 허용한다.
- 데이터, 평가지표, 대회 규칙, 사용 가능한 외부 자원 또는 계산 예산이 바뀌면 기존 모델 계획을 무효화하고 다시 설계한다.
- 구현·실험 담당은 `experiment-plan.csv`의 순서와 고정 조건을 따른다. 계획에서 벗어난 변경은 이유와 결과를 `model-decision-log.md`에 기록한다.
- 설계 중 데이터 근거가 부족하거나 서로 모순되면 임의로 가정하지 않는다. `MODEL_PLAN: BLOCKED`로 표시하고 `hackathon_data_analyst`에 필요한 추가 분석을 구체적으로 요청한다.

## 실험·검증·개선 순환

1. `hackathon_experiment_engineer`는 `MODEL_PLAN: READY`와 승인된 `experiment-plan.csv`만 구현하고 실행하며, 실험 묶음을 마치면 `EXPERIMENT_BATCH: COMPLETE` 또는 `EXPERIMENT_BATCH: BLOCKED`를 기록한다.
2. 실험 엔지니어는 실행별 설정, 코드·데이터 버전, seed, fold, 환경, 비용, OOF 예측, 지표와 실패를 재현 가능한 형태로 기록한다.
3. `hackathon_validation_auditor`는 구현과 독립적으로 지표, split, OOF, 누수, seed 안정성, 규칙 준수와 추론 일관성을 검증하고 `EXPERIMENT_AUDIT: PASS` 또는 `EXPERIMENT_AUDIT: BLOCKED`를 기록한다.
4. 감사 결과가 `EXPERIMENT_AUDIT: BLOCKED`이면 결과를 폐기하거나 보류하고, 발견 유형에 따라 데이터 분석가·모델 아키텍트·실험 엔지니어에게 수정 책임을 돌려보낸다.
5. `EXPERIMENT_AUDIT: PASS`를 받은 결과만 `hackathon_model_architect`가 해석한다. 모델 아키텍트는 가설의 채택·기각, 오류 패턴, 비용 대비 이득을 바탕으로 개선 방향과 다음 실험 묶음을 설계한다.
6. 모델 아키텍트는 완료된 실험 행을 수정하지 않고 새 실험을 `experiment-plan.csv`에 추가하며, 결정과 계획 변경 이유를 `model-decision-log.md`와 iteration review에 기록한다.
7. 개선 루프는 사전에 정한 중단 기준, 계산 예산, 마감 또는 검증상 의미 있는 개선의 소진 중 하나에 도달하면 종료한다.
8. Public 리더보드 점수는 검증 설계·모델 선택·가중치 튜닝을 사후 정당화하는 근거로 사용하지 않는다.

결과 기반 최적화·수정·개선 방향의 최종 책임자는 `hackathon_model_architect`다. 실험 엔지니어는 승인된 계획을 실행하고, 검증 감사관은 결과의 신뢰성을 판정하며, 두 역할은 임의로 다음 모델을 선택하지 않는다.
