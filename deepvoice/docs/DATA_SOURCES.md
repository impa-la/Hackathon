# Data source register

Every training source must be entered in `DATA_SOURCES.csv` before use

Record the exact source URL, license, version or retrieval date, local path, and intended use This register is required because the competition rules require every external dataset and pretrained model source to be disclosed during the second evaluation

Do not commit downloaded audio or private evaluation data to Git
